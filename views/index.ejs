<!DOCTYPE html>
<html lang="pt_br">

    <%- include('partials/head.ejs') %>
    
<body>
    <%- include('partials/landingHeader.ejs') %>
    <main>
        <section id="landing-page">
            <div id="landing-text">
                <h1>Descubra o que seu PC é capaz de fazer!</h1>
                <a href="#pc-form" class="p-button">Comece agora!</a>
            </div>
        </section>
        <h1 class="divisor">Comece por aqui</h1>
        <section >
            <form action="" id="pc-form">
                <h2>Meu PC roda?</h2>
                <p>Preencha os campos abaixo para ver quais jogos seu PC pode rodar</p>
                <label for="processador">Processador</label>
                <select name="processadores" id="processadores" required>
                    <option value="" selected disabled hidden></option>
                    <% for( let processador of processadores){ %>
                        <option value="<%= processador.id %> "><%= processador.modelo %></option>
                    <% } %>
                </select>
                <label for="ram">Memória RAM</label>
                <input type="number" min="1" max="16" name="ram" id="ram" required>
                <label for="armazenamento">Armazenamento(em GB)</label>
                <input type="number" min="120" max="4000" name="armazenamento" id="armazenamento" required>
                <label for="placa-video">Placa de vídeo</label>
                <select name="placaVideo" id="placaVideo">
                    <option value="" selected disabled hidden></option>
                    <% for( let placaVideo of placasVideo){ %>
                        <option value="<%= placaVideo.id %> "><%= placaVideo.modelo %></option>
                    <% } %>
                </select>
                <a href="/suporte">Não encontrei o componente que queria</a>
                <input type="submit" class="p-button">
            </form>
        </section>
    </main>
</body>
</html>